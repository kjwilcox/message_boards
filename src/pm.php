<?php
/*require('include.php');
$logged = login();
db_connect();

if (!$logged) { die_('You must be logged in to access Private Messages.'); }

list($key) = each($_GET);
switch($key) {
	case 'inbox':
	
	$result = query("SELECT * FROM pms WHERE Status IN () ");
	
	
	
	
	
	
	
	
	
	
	
	
	
	break;
	case 'read': break;
	case 'sent': break;
	case 'delete': break;
	case 'write': break;
	default: die_('Invalid Page');
}
*/
?>